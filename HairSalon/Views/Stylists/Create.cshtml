@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<h4>Add a new Stylist here!</h4>
@using (Html.BeginForm())
{
  <h3>Stylist Name:</h3>
  @Html.TextBoxFor(model => model.StylistName)
  <br>
  <input type="submit" value="Add new Stylist" />
}
<p>@Html.ActionLink("Show all Stylists", "Index")</p>